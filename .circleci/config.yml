version: 2.1
jobs:
  bun-vite-test:
    docker:
      - image: oven/bun:latest
    steps:
      - checkout
      - run:
          name: "bun-vite-test"
          command: bun test
  bun-vite-build:
    docker:
      - image: oven/bun:latest
    steps:
      - checkout
      - run:
          name: "bun-vite-build"
          command: bun run build
      - store_artifact:
          path: template-vite-react-swc-ts.txt
workflows:
  test-and-build:
    jobs:
      - bun-vite-test
      - bun-vite-build:
          requires:
            - bun-vite-test
